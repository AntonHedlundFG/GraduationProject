class USInRangeOfValidTarget : UCConsideration
{
    float Evaluate(FCAIContext Context)
    {
        // if (Context.CurrentUnit == nullptr || Context.PlayerUnits.Num() == 0)
        // {
        //     return 0.0f;
        // }

        // float distance = 0; // If current Ability has any of player units in valid tiles
        // if (distance < 10f)
        // {
        //     return 1f;
        // }
        // else
        // {
        //     return 0f;
        // }
        return 0.0f;
    }
}